FT = lambda: [(f.write(CHAR % CHAR), f.close()) for f in [open(NAME, mode="w")]]
NAME = "Grace_kid.py"
CHAR = 'FT = lambda: [(f.write(CHAR %% CHAR), f.close()) for f in [open(NAME, mode="w")]]\nNAME = "Grace_kid.py"\nCHAR = %r\n# Lets the hack begin\nFT()\n'
# Lets the hack begin
FT()
